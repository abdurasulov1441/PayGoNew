import 'package:flutter/material.dart';

class NewsTruck extends StatelessWidget {
  const NewsTruck({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}